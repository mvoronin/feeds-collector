database:
    path: "../data/feeds.db"

# Default interval for updating feeds
# available time intervals: m, h
feeds_update_interval: "20m"

logging:
  info_log: "info.log"
  error_log: "error.log"

server:
  port: "8080"
